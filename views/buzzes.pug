doctype html
html
  head
    title=title
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(rel="stylesheet" href='/style.css')
  body.buzzes

    div.container
      ol.js-buzzes
        each buzz in buzzes
          li=`${buzz.user.name}`

    script(src='/socket.io/socket.io.js')
    script(src='/buzzes.js')
